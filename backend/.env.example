SECRET_KEY=change-me-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
DB_NAME=family_tree
DB_USER=
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Optional: leave blank to store uploaded photos on local disk (dev default).
# Set all three (from https://cloudinary.com/console) to upload to Cloudinary
# instead -- needed in production since local disk storage doesn't survive
# redeploys/isn't served via a CDN.
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
